---
phase: 02-job-health-monitoring
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - job_monitor/ui/routes/_sidebar/job-health.tsx
  - job_monitor/ui/components/job-health-table.tsx
  - job_monitor/ui/components/job-health-row.tsx
  - job_monitor/ui/components/duration-chart.tsx
  - job_monitor/ui/components/priority-badge.tsx
  - job_monitor/ui/components/status-indicator.tsx
  - job_monitor/ui/lib/health-utils.ts
  - job_monitor/ui/components/ui/collapsible.tsx
  - job_monitor/ui/components/ui/tabs.tsx
  - job_monitor/ui/components/ui/badge.tsx
  - job_monitor/ui/components/ui/tooltip.tsx
autonomous: false
requirements: [JOB-01, JOB-02, JOB-03, JOB-04]

must_haves:
  truths:
    - "Platform user can view job health dashboard with success rates"
    - "Jobs are sorted problem-first (P1 at top, then P2, P3, healthy)"
    - "Tabs allow switching between 7-day and 30-day views"
    - "Row expansion shows recent runs and duration chart"
    - "Traffic light indicator shows green/yellow/red based on success rate"
    - "P1/P2/P3 badges are visible for priority jobs"
    - "Duration anomalies show warning icon with tooltip"
    - "Retry count badge appears when retries > 2"
  artifacts:
    - path: "job_monitor/ui/routes/_sidebar/job-health.tsx"
      provides: "Job health dashboard page"
      min_lines: 50
    - path: "job_monitor/ui/components/job-health-table.tsx"
      provides: "Expandable table with job rows"
      contains: "Collapsible"
    - path: "job_monitor/ui/components/duration-chart.tsx"
      provides: "Recharts duration trend visualization"
      contains: "LineChart"
    - path: "job_monitor/ui/components/priority-badge.tsx"
      provides: "P1/P2/P3 badge component"
      contains: "Badge"
  key_links:
    - from: "job_monitor/ui/routes/_sidebar/job-health.tsx"
      to: "/api/health-metrics"
      via: "TanStack Query fetch"
      pattern: "useQuery.*health-metrics"
    - from: "job_monitor/ui/components/job-health-row.tsx"
      to: "/api/health-metrics/{job_id}/details"
      via: "fetch on expand"
      pattern: "useQuery.*details"
    - from: "job_monitor/ui/components/duration-chart.tsx"
      to: "recharts"
      via: "LineChart import"
      pattern: "import.*LineChart.*from.*recharts"
---

<objective>
Create the job health dashboard UI with expandable rows, traffic light indicators, priority badges, and duration trend charts.

Purpose: Platform team needs a visual interface to monitor job health at a glance, identify problems quickly (problem-first sorting), and drill down into details (expand rows).
Output: Job Health dashboard page accessible via sidebar navigation with complete health monitoring UI.
</objective>

<execution_context>
@/Users/laurent.prat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laurent.prat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-job-health-monitoring/02-CONTEXT.md
@.planning/phases/02-job-health-monitoring/02-RESEARCH.md
@.planning/phases/02-job-health-monitoring/02-01-SUMMARY.md
@.planning/phases/02-job-health-monitoring/02-02-SUMMARY.md

# Existing UI code
@job_monitor/ui/routes/_sidebar/dashboard.tsx
@job_monitor/ui/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and add shadcn/ui components</name>
  <files>job_monitor/ui/components/ui/collapsible.tsx, job_monitor/ui/components/ui/tabs.tsx, job_monitor/ui/components/ui/badge.tsx, job_monitor/ui/components/ui/tooltip.tsx</files>
  <action>
From the job_monitor/ui directory, install required dependencies:

```bash
cd job_monitor/ui
npx shadcn@latest add collapsible tabs badge tooltip table --yes
npm install recharts lucide-react
```

This adds the shadcn/ui primitives needed for:
- Collapsible: Expandable row behavior
- Tabs: 7-day/30-day toggle
- Badge: P1/P2/P3 priority badges
- Tooltip: Duration anomaly explanations
- Table: Base table components (if not already present)

Also install:
- recharts: Duration trend line charts
- lucide-react: Icons (ChevronDown, AlertTriangle, CheckCircle, Clock)
  </action>
  <verify>
cd /Users/laurent.prat/Documents/lpdev/databricks_job_monitoring/job_monitor/ui && npm ls recharts lucide-react && ls -la components/ui/collapsible.tsx components/ui/tabs.tsx components/ui/badge.tsx
  </verify>
  <done>shadcn/ui components (collapsible, tabs, badge, tooltip) installed and recharts + lucide-react dependencies added</done>
</task>

<task type="auto">
  <name>Task 2: Create utility components and helpers</name>
  <files>job_monitor/ui/lib/health-utils.ts, job_monitor/ui/components/status-indicator.tsx, job_monitor/ui/components/priority-badge.tsx</files>
  <action>
Create health-utils.ts with:
- STATUS_THRESHOLDS constant: { green: 90, yellow: 70, red: 0 }
- getStatusColor(successRate: number): "green" | "yellow" | "red"
- formatDuration(seconds: number): string (e.g., "2h 15m" or "45m")
- formatTimeAgo(date: Date | string): string (e.g., "2h ago", "1d ago")

Create status-indicator.tsx component:
- Props: { successRate: number }
- Renders traffic light circle (div with rounded-full) + percentage text
- Uses STATUS_THRESHOLDS for color: bg-green-500, bg-yellow-500, bg-red-500
- Example: [green dot] 94.5%

Create priority-badge.tsx component:
- Props: { priority: "P1" | "P2" | "P3" | null }
- Returns null if priority is null
- P1: red badge (bg-red-600 text-white)
- P2: orange badge (bg-orange-500 text-white)
- P3: yellow badge (bg-yellow-500 text-black)
- Use shadcn/ui Badge with custom className

Follow patterns from 02-RESEARCH.md code examples.
  </action>
  <verify>
cd /Users/laurent.prat/Documents/lpdev/databricks_job_monitoring/job_monitor/ui && npx tsc --noEmit lib/health-utils.ts components/status-indicator.tsx components/priority-badge.tsx 2>&1 || echo "Type check (may have import errors before full build)"
  </verify>
  <done>
health-utils.ts with threshold constants and formatting functions exists.
StatusIndicator component renders traffic light with percentage.
PriorityBadge component renders colored P1/P2/P3 badges.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create duration chart and expanded details components</name>
  <files>job_monitor/ui/components/duration-chart.tsx, job_monitor/ui/components/job-expanded-details.tsx</files>
  <action>
Create duration-chart.tsx:
- Props: { data: Array<{run_time: string, duration_seconds: number}>, medianBaseline: number }
- Use Recharts ResponsiveContainer, LineChart, Line, XAxis, YAxis, Tooltip, ReferenceLine
- Add baseline reference line (dashed gray) at medianBaseline
- Add anomaly threshold line (dashed red) at 2x medianBaseline
- Custom dot renderer: larger red dots for anomalous points (duration > 2x baseline)
- X-axis: dates formatted as locale date string
- Y-axis: duration formatted as minutes (Math.round(v / 60) + "m")
- Height: 200px

Create job-expanded-details.tsx:
- Props: { jobId: string, jobName: string }
- Fetch expanded details using TanStack Query: useQuery for /api/health-metrics/{jobId}/details
- Display layout with two columns:
  - Left: Recent Runs list (last 10)
    - Each run: start time, duration (formatted), result badge (SUCCESS/FAILED), anomaly icon if is_anomaly
  - Right: Metrics Summary
    - Duration Stats table: Median, P90, Last Run, Max
    - Comparison to baseline: "Last run: 45m (2.1x baseline)" with warning icon if anomaly
    - Retry count: "Retries: N in last 7 days" with badge if > 2
    - Failure reasons: list of distinct error messages (if any)
- Show DurationChart below the two columns
- Handle loading state with skeleton
- Handle insufficient data state: "Need 5+ runs for baseline"

Follow patterns from 02-RESEARCH.md code examples.
  </action>
  <verify>
cd /Users/laurent.prat/Documents/lpdev/databricks_job_monitoring/job_monitor/ui && npx tsc --noEmit components/duration-chart.tsx components/job-expanded-details.tsx 2>&1 || echo "Type check complete"
  </verify>
  <done>
DurationChart component renders Recharts LineChart with baseline reference lines and anomaly highlighting.
JobExpandedDetails component fetches and displays recent runs, metrics summary, and duration chart.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create job health table with expandable rows</name>
  <files>job_monitor/ui/components/job-health-row.tsx, job_monitor/ui/components/job-health-table.tsx</files>
  <action>
Create job-health-row.tsx:
- Props: { job: JobHealth (matching JobHealthOut from API) }
- Use shadcn/ui Collapsible for expand/collapse
- Collapsed row (TableRow) shows:
  - Expand button (ChevronDown icon, rotates 180 on open)
  - Job name
  - StatusIndicator (traffic light + percentage)
  - PriorityBadge (P1/P2/P3)
  - "Last run: Xh ago" using formatTimeAgo
  - Retry badge if retry_count > 2: "N retries" in orange
- Expanded row (CollapsibleContent with TableRow, colSpan full width):
  - JobExpandedDetails component

Create job-health-table.tsx:
- Props: { jobs: JobHealth[], isLoading: boolean }
- Use shadcn/ui Table, TableHeader, TableBody, TableRow, TableHead, TableCell
- Column headers: "", Job Name, Status, Priority, Last Run
- Map jobs to JobHealthRow components
- Show skeleton rows during loading (3 skeleton rows)
- Show "No jobs found" message when jobs array is empty

Ensure rows maintain expand state independently (each row has its own isOpen state).
  </action>
  <verify>
cd /Users/laurent.prat/Documents/lpdev/databricks_job_monitoring/job_monitor/ui && npx tsc --noEmit components/job-health-row.tsx components/job-health-table.tsx 2>&1 || echo "Type check complete"
  </verify>
  <done>
JobHealthRow component renders collapsible row with expand/collapse behavior.
JobHealthTable component renders table with all job rows and loading states.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create job health dashboard page with tabs</name>
  <files>job_monitor/ui/routes/_sidebar/job-health.tsx</files>
  <action>
Create job-health.tsx page:
- Use TanStack Query to fetch from /api/health-metrics with days parameter
- Tabs component for 7-day/30-day toggle:
  - TabsList with two TabsTrigger: "7 Days", "30 Days"
  - Active tab determines days query param (default: 7)
- Page header: "Job Health" title
- JobHealthTable component with fetched jobs data
- Handle query states:
  - Loading: pass isLoading=true to table
  - Error: show error message with retry button
  - Empty: handled by table component

Add route to sidebar navigation in app/routes layout if needed.

Use TanStack Query with:
- queryKey: ['health-metrics', { days }]
- staleTime: 5 * 60 * 1000 (5 minutes - system tables have 5-15 min latency)
- refetchOnWindowFocus: true

TypeScript interfaces should match API response (JobHealthOut fields).
  </action>
  <verify>
cd /Users/laurent.prat/Documents/lpdev/databricks_job_monitoring/job_monitor/ui && npx tsc --noEmit routes/_sidebar/job-health.tsx 2>&1 || echo "Type check complete"
  </verify>
  <done>
Job health dashboard page exists at routes/_sidebar/job-health.tsx.
Tabs switch between 7-day and 30-day views.
Page fetches from /api/health-metrics and renders JobHealthTable.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verify job health dashboard functionality</name>
  <files>N/A - verification only</files>
  <action>
Human verification of the complete job health monitoring dashboard. All automated tasks have been completed in Tasks 1-5. This checkpoint confirms the visual and functional correctness of the implementation.

What was built:
- Traffic light indicators (green/yellow/red) based on success rate thresholds
- P1/P2/P3 priority badges for failing jobs
- Problem-first sorting (failing jobs at top)
- Expandable rows showing recent runs and duration chart
- 7-day/30-day tab toggle
- Duration trend visualization with anomaly highlighting
- Retry count badges
  </action>
  <verify>
1. Start the development server:
   cd /Users/laurent.prat/Documents/lpdev/databricks_job_monitoring
   source .venv/bin/activate
   uvicorn job_monitor.backend.app:app --reload &
   cd job_monitor/ui && npm run dev

2. Open browser to http://localhost:5173/job-health (or the UI dev server URL)

3. Verify the dashboard shows:
   - Jobs sorted by priority (P1 first, then P2, P3, healthy last)
   - Traffic light indicators with percentages
   - P1/P2/P3 badges on problem jobs
   - "Last run: Xh ago" timestamps

4. Click on a job row to expand:
   - Recent runs list appears
   - Duration chart shows with baseline reference line
   - Metrics summary shows duration stats
   - Retry count displays

5. Toggle between 7-day and 30-day tabs:
   - Data refreshes with new time window
   - Success rates update accordingly

6. Look for anomaly indicators:
   - Warning icons on duration anomalies (>2x baseline)
   - Retry badges on jobs with >2 retries
  </verify>
  <done>Human confirms dashboard displays correctly with all features: traffic lights, priority badges, expandable rows, duration charts, tabs, and anomaly indicators</done>
</task>

</tasks>

<verification>
- [ ] Job health dashboard page loads at /job-health route
- [ ] Jobs are displayed sorted by priority (P1 > P2 > P3 > healthy)
- [ ] Traffic light indicators show correct colors based on success rate
- [ ] P1/P2/P3 badges appear on appropriate jobs
- [ ] Tabs switch between 7-day and 30-day views
- [ ] Clicking a row expands to show detailed view
- [ ] Duration chart renders with baseline and anomaly threshold lines
- [ ] Recent runs list shows last 10 runs with result states
- [ ] Retry count badge appears when retries > 2
- [ ] Duration anomalies have warning icon with tooltip
</verification>

<success_criteria>
1. Dashboard displays job health list with problem-first sorting
2. Traffic light indicators work (green >= 90%, yellow 70-89%, red < 70%)
3. P1/P2/P3 badges correctly identify consecutive failures (P1), single failures (P2), yellow zone (P3)
4. Expandable rows show recent runs and duration trend chart
5. 7-day/30-day tabs toggle time window
6. Duration anomalies (>2x baseline) are visually flagged
7. Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/02-job-health-monitoring/02-03-SUMMARY.md`
</output>
