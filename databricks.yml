bundle:
  name: job-monitor

sync:
  include:
    - job_monitor/ui/dist/**

targets:
  dev:
    mode: development
    default: true
    workspace:
      profile: LPT_FREE_EDITION

resources:
  apps:
    job-monitor:
      name: job-monitor
      description: "Databricks Job Monitoring Framework"
      source_code_path: .
